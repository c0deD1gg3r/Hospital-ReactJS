import './Button.css';

const Button = () => {
  return (
    <div style={{ display: 'flex' }}>
      <div>
        <button className='button'>ЗАПИСАТЬСЯ НА ПРИЁМ</button>
      </div>
    </div>
  );
};

export default Button;